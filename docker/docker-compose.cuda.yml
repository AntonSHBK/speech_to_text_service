name: speech_to_text_service

services:
  speech-to-text-service:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.gpu
    volumes:
      - ../:/app/
      - ../logs:/app/logs
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    env_file:
      - ./.env
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    ports:
      - "8000:8000"
    depends_on:
      - redis
    networks:
      - service_network
    restart: unless-stopped

  worker:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.gpu
    volumes:
      - ../:/app/
      - ../logs:/app/logs
    command: celery -A app.celery_worker.celery_app worker --loglevel=info --concurrency=1
    env_file:
      - ./.env
    environment:
      - DEVICE=cuda
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    gpus: all
    depends_on:
      - redis
    networks:
      - service_network
    restart: unless-stopped

  flower:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile.gpu
    command: celery -A app.celery_worker.celery_app flower --port=5555 --basic_auth=${FLOWER_BASIC_AUTH} --persistent=True --db=/data/flower.db
    env_file:
      - ./.env
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    volumes:
      - flower_data:/data
    depends_on:
      - redis
      - worker
    ports:
      - "5555:5555"
    networks:
      - service_network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - service_network
    restart: unless-stopped

networks:
  service_network:
    driver: bridge
    # external: true
    # name: shared_net

volumes:
  redis_data:
  flower_data:
